/* Right-side overlay for Cliche_Air
 * - Provides key matrix pin mapping for right half
 * - Disables encoder (left-only)
 * - Enables trackball (right-only)
 *
 * NOTE: Verify the pin numbers (P0.xx) against your PCB schematic before building.
 */

#include "cliche_air.dtsi"

/* Key matrix (Left) */
&kscan0 {
    /* Right hand key matrix */
        col-gpios
            = <&xiao_d 10 GPIO_ACTIVE_HIGH>
            , <&xiao_d 9 GPIO_ACTIVE_HIGH> 
            , <&xiao_d 8 GPIO_ACTIVE_HIGH> 
            , <&xiao_d 7 GPIO_ACTIVE_HIGH> 
            , <&gpio0 10 GPIO_ACTIVE_HIGH>
            , <&gpio0 9 GPIO_ACTIVE_HIGH>
            ;
};

/* Disable encoder on right side */
&left_encoder {
    status = "okay";
};